{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "lib": ["ES2022", "DOM"],
    // Removed explicit "types" to avoid missing type definition errors for node/electron
    // Types will be resolved automatically if @types packages are installed.
    
  },
  "include": ["src/preload/**/*.ts", "src/shared/**/*.ts"]
}
