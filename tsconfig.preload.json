{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "lib": ["ES2022", "DOM"]
    // Types are resolved automatically from installed @types packages.
  },
  "include": ["src/preload/**/*.ts", "src/shared/**/*.ts"]
}
